doctype html
html(lang='zh-CN')
  head
    meta(charset='utf-8')
    link(rel='stylesheet',href='https://unpkg.com/semantic-ui@2.2.13/dist/semantic.css')
    title jade 高级用法
  body
    div.ui.container(style='padding-top: 100px')
      h1(class=classArr, class='padded') 我的名字: #{name}
      div.box(style=styleJson)
        |div
        |aaa
        |123
        |来点我  
      include segment.html
      div.ui.positive.message
        -var a = 5;
        -var b = 20;
        span a 的值是 #{a} b的值是 
        span=b
        |  结果是 #{a+b}
        -var arr = [1,3,4,,5,6];
        -for(var i=0; i<arr.length; i++)
          span=arr[i]
  script.
    window.onload = function () {
      var con = document.querySelector('.box');
      con.onclick = function () {
        this.style.backgroundColor = this.style.backgroundColor == 'red' ? 'blue' : 'red';
      }
    }
